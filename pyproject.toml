[project]
name = "bunker-monorepo"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = []

[tool.uv.workspace]
members = [
    "services/*",
    "shared"
]

[tool.uv.sources]
shared = { workspace = true }

[dependency-groups]
dev = [
    "pytest-cov>=7.0.0",
]
